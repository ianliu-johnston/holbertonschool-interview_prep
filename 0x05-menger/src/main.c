#include "menger.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	menger(0);
	getchar();
	menger(1);
	getchar();
	menger(2);
	getchar();
	menger(3);
	getchar();
	return (0);
}
